/**
 * 
 * @interface
 */
class WinStrategyInterface {
	/**
	 * Return null if no winner yet
	 * Return GameObject if winner found
	 * Return false if no winner and game finished
	 * @param {Array} game_objects 
	 */
	get_winner(game_objects) {
		// Nothing
	}
}
